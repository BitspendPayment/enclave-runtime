// World we generate host bindings for. We only need the filesystem
// interfaces — wasi:io, wasi:clocks, etc. are reused from `wasmtime-wasi`.
package s3fs:host;

world enclave-runtime {
    import wasi:filesystem/types@0.2.6;
    import wasi:filesystem/preopens@0.2.6;
}
